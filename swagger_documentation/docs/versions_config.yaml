# API Documentation Versions Configuration
# This file defines all supported API versions and their metadata
# Similar to apiv1's versioning.py but for documentation purposes

# Default version to display when no version is selected
default_version: "2.0"

# Supported versions metadata
versions:
  "2.0":
    deprecated: false
    description: "Current stable API version"

  "2.3":
    deprecated: false
    description: "Latest API version with enhanced article search and improved related materials"
